# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/

# turbo
.turbo/

# husky
.husky/_/

# test / coverage
coverage/
*.lcov

# playwright mcp (снимки/логи ручной проверки UI — вне git)
.playwright-mcp/

# ralph loop — рантайм-счётчик итераций
.claude/ralph.iterations.json

# ralph loop — кэш переведённых slug'ов для имён веток (пишется во время прогона)
.claude/ralph-branch-names.json

# ralph loop — сайдбенд с расходом токенов/стоимостью последнего прогона claude
.claude/ralph-session.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.*
!.env.example

# uploaded meeting files (бинарники — вне git, только метаданные в БД)
apps/api/uploads/

# editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.tsbuildinfo
next-env.d.ts
